name: Trigger Pages Rebuild

on:
  push:
    branches: [ main ]

jobs:
  rebuild:
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    
    steps:
    - name: Trigger rebuild
      run: echo "Triggering Pages rebuild"